{
  "meta": { "notes": "Patterns first, then keyword contains, then TF-IDF, then LLM." },

  "type_text": {
    "patterns": ["(?i)^(type|write|dictate)\\s+(?P<text>.+)$"],
    "examples": ["type hello team", "write this down"]
  },

  "tab_next_field": {
  "patterns": [
    "(?i)^subject$",
    "(?i)^body$"
  ],
  "examples": ["subject", "body", "next field"]
},

  "open_gmail": {
    "patterns": ["(?i)^(?:open|go\\s+to|check)\\s+(?:my\\s+)?(?:gmail|e-?mail|mail|inbox)$"],
    "examples": ["open gmail", "go to gmail", "open my email"]
  },

  "set_browser": {
    "patterns": [
      "(?i)^(?:set|use|prefer)\\s+(?P<browser>safari|chrome)\\s+(?:as\\s+)?(?:my\\s+)?browser$",
      "(?i)^switch\\s+browser\\s+to\\s+(?P<browser>safari|chrome)$",
      "(?i)^switch\\s+to\\s+(?P<browser>safari|chrome)$"
    ],
    "examples": [
      "use safari as my browser",
      "set browser chrome",
      "switch to safari"
    ],
    "slots": { "browser": "(safari|chrome)" }
  },

  "compose_email": {
    "patterns": [
      "(?i)^compose\\s+(?:an\\s+)?email\\s+to\\s+(?P<to>[^\\s]+)\\s+subject\\s+(?P<subject>.+?)\\s+body\\s+(?P<body>.+)$",
      "(?i)^email\\s+(?P<to>[^\\s]+)\\s+subject\\s+(?P<subject>.+?)\\s+body\\s+(?P<body>.+)$"
    ],
    "examples": [
      "compose email to alice@example.com subject project update body pushing the deck tonight",
      "email bob@foo.io subject check in body did you see my note"
    ],
    "slots": {
      "to": "([^\\s]+)",
      "subject": "(.+)",
      "body": "(.+)"
    }
  },

  "send_email": {
    "patterns": [
      "(?i)^send\\s+email$",
      "(?i)^send$",
      "(?i)^send\\s+email\\s+to\\s+(?P<to>[^\\s]+)\\s+subject\\s+(?P<subject>.+?)\\s+body\\s+(?P<body>.+)$"
    ],
    "examples": [
      "send email",
      "send",
      "send email to team@acme.com subject standup body blocker on api rollout"
    ],
    "slots": {
      "to": "([^\\s]+)",
      "subject": "(.+)",
      "body": "(.+)"
    }
  },

  "start_presentation": {
    "patterns": [
      "(?i)^(start|begin|open|play|launch|run)\\s+(?:the\\s+)?(presentation|slideshow|slide\\s*show|slides)$",
      "(?i)^present(?:\\s+(?:the\\s+)?(?:slides?|deck|presentation))?$",
      "(?i)^(?:go\\s+to\\s+)?google\\s+slides$"
    ],
    "examples": ["open presentation", "start presentation", "begin slideshow", "play slides", "present", "present slides", "google slides"]
  },

  "new_tab": {
    "patterns": ["(?i)^(?:open\\s+(?:a\\s+)?(?:new\\s+)?tab|new\\s+tab)$"],
    "examples": ["new tab", "open a new tab", "open a tab"]
  },

  "open_url": {
    "patterns": ["(?i)^(open|go to|navigate to)\\s+(?P<url>[^\\s]+)$"],
    "examples": ["open twitter.com", "go to github", "navigate to notion"]
  },

  "open_app": {
    "patterns": ["(?i)^(open|launch|start)\\s+(?P<app>.+)$"],
    "examples": ["open chrome", "launch safari", "start keynote", "open edge", "open brave", "open arc"]
  },

  "close_tab": { "examples": ["close tab", "close this tab"] },
  "next_tab": { "examples": ["next tab"] },
  "previous_tab": { "examples": ["previous tab"] },
  "reload_tab": { "examples": ["reload", "refresh"] },

  "scroll_down": { "examples": ["scroll down", "down a bit", "page down", "next"] },
  "scroll_up": { "examples": ["scroll up", "up a bit", "page up"] },
  "scroll_top": { "examples": ["scroll to top", "go to top"] },
  "scroll_bottom": { "examples": ["scroll to bottom", "go to bottom"] },

  "tab_key": { "examples": ["tab", "next field"] },
  "shift_tab_key": { "examples": ["shift tab", "previous field"] },
  "enter_key": { "examples": ["press enter", "hit return"] },
  "escape_key": { "examples": ["press escape", "cancel"] },

  "backspace": { "examples": ["backspace", "delete previous"] },
  "delete_forward": { "examples": ["delete", "delete forward"] },


  "select_all": { "examples": ["select all"] },
  "copy": { "examples": ["copy"] },
  "paste": { "examples": ["paste"] },
  "cut": { "examples": ["cut"] },
  "undo": { "examples": ["undo"] },
  "redo": { "examples": ["redo"] },

  "bold": { "examples": ["bold", "make it bold"] },
  "italic": { "examples": ["italic"] },
  "underline": { "examples": ["underline"] },

  "find": { "examples": ["find", "search on page"] },
  "find_next": { "patterns": ["(?i)^find\\s+next$"], "examples": ["find next"] },
  "find_previous": { "patterns": ["(?i)^find\\s+(?:previous|prev)$"], "examples": ["find previous"] },

  "arrow_left": { "examples": ["left"] },
  "arrow_right": { "examples": ["right"] },
  "arrow_up": { "examples": ["up"] },
  "arrow_down": { "examples": ["down"] },

  "word_left": { "patterns": ["(?i)^word\\s+left$"], "examples": ["word left"] },
  "word_right": { "patterns": ["(?i)^word\\s+right$"], "examples": ["word right"] },
  "line_start": { "examples": ["line start", "home"] },
  "line_end": { "examples": ["line end", "end"] },

  "zoom_in": { "examples": ["zoom in"] },
  "zoom_out": { "examples": ["zoom out"] },
  "zoom_reset": { "examples": ["reset zoom"] },

  "exit_presentation": { "examples": ["exit presentation", "stop slideshow"] },
  "next_slide": { "examples": ["next slide", "advance slide", "go forward"] },
  "previous_slide": { "examples": ["previous slide", "go back slide"] },
  "first_slide": { "examples": ["first slide"] },
  "last_slide": { "examples": ["last slide"] },

  "new_window": { "examples": ["new window"] },
  "close_window": { "examples": ["close window", "close this window"] },
  "minimize_window": { "examples": ["minimize window"] },
  "switch_app": { "examples": ["switch app", "app switcher"] },
  "repeat_last": { "examples": ["repeat last", "do that again"] }
}
